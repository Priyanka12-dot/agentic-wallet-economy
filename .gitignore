# ── Secrets — NEVER commit these ─────────────────────────────────
.env
.env.local
.env.*.local
.env
.env.example
agent-core/.env
dashboard/.env
deployed-addresses.json
logs/
node_modules/

# Build artifacts and logs
artifacts/
cache/
logs/
node_modules/
dashboard/dist/
dashboard/node_modules/
agent-core/node_modules/

# ── Node ──────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── TypeScript build output ───────────────────────────────────────
dist/
build/
*.js.map
*.d.ts

# ── Hardhat ───────────────────────────────────────────────────────
cache/
artifacts/
typechain-types/
coverage/
coverage.json

# ── Deployed addresses (safe to commit if you want — optional) ────
# deployed-addresses.json

# ── Agent logs ────────────────────────────────────────────────────
logs/

# ── Dashboard build ───────────────────────────────────────────────
dashboard/dist/
dashboard/.vite/

# ── OS & editor ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# ── Byreal CLI — never commit actual private keys ─────────────────
agent-core/byreal-config/agent-wallets.json
# ^ Uncomment the line above if you want to ignore wallet config.
#   The template (with placeholder values) is safe to commit.
